# ⚡ Быстрый деплой KAMHUB за 5 минут

## 🎯 Вариант 1: Vercel (Самый простой - РЕКОМЕНДУЕТСЯ)

### Шаг 1: Получите Telegram Bot Token

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду: `/newbot`
3. Придумайте имя бота (например: "Kamhub Notifications")
4. Придумайте username (например: "kamhub_notify_bot")
5. Скопируйте **токен** (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Получите Chat ID

1. Отправьте любое сообщение вашему новому боту
2. Откройте в браузере (замените YOUR_TOKEN на ваш токен):
   ```
   https://api.telegram.org/botYOUR_TOKEN/getUpdates
   ```
3. Найдите число после `"chat":{"id":` (например: `123456789`)
4. Сохраните это число - это ваш Chat ID

### Шаг 3: Деплой на Vercel

1. **Откройте:** https://vercel.com
2. **Войдите** через GitHub аккаунт
3. **Нажмите:** "Add New" → "Project"
4. **Выберите репозиторий:** `pospkam/HabKam`
5. **Framework:** Next.js (определится автоматически)
6. **Environment Variables** - добавьте 3 переменные:
   
   ```
   Имя: TELEGRAM_BOT_TOKEN
   Значение: (ваш токен из Шага 1)
   
   Имя: TELEGRAM_CHAT_ID  
   Значение: (ваш chat id из Шага 2)
   
   Имя: NEXT_PUBLIC_SITE_URL
   Значение: https://kamhub.vercel.app (или ваш URL)
   ```

7. **Нажмите "Deploy"**
8. **Подождите 2-3 минуты** ☕

### Шаг 4: Проверьте работу

1. Откройте URL вашего сайта (типа `https://kamhub-xxx.vercel.app`)
2. Перейдите в раздел "Туры"
3. Выберите любой тур
4. Нажмите "Забронировать"
5. Заполните форму и отправьте
6. **Проверьте Telegram** - должно прийти уведомление! 🎉

---

## 🎯 Вариант 2: Netlify

### Быстрый деплой:

1. Откройте: https://app.netlify.com
2. Войдите через GitHub
3. "Add new site" → "Import an existing project"
4. Выберите `pospkam/HabKam`
5. Добавьте переменные окружения (см. выше)
6. Deploy site

---

## 🎯 Вариант 3: Docker на своём сервере

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/pospkam/HabKam.git
cd HabKam

# 2. Создайте .env.local файл
cat > .env.local << EOF
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
NEXT_PUBLIC_SITE_URL=http://ваш-сервер:3000
EOF

# 3. Запустите с Docker
docker-compose up -d

# Сайт доступен на http://ваш-сервер:3000
```

---

## 🎯 Вариант 4: PM2 на VPS

```bash
# 1. Клонируйте и установите
git clone https://github.com/pospkam/HabKam.git
cd HabKam
npm install

# 2. Создайте .env.local
nano .env.local
# (вставьте переменные и сохраните: Ctrl+O, Enter, Ctrl+X)

# 3. Соберите проект
npm run build

# 4. Запустите с PM2
npm install -g pm2
pm2 start npm --name "kamhub" -- start
pm2 save
pm2 startup

# Сайт доступен на http://localhost:3000
```

---

## ✅ Чек-лист проверки

После деплоя проверьте:

- [ ] Главная страница открывается
- [ ] Список туров отображается (`/tours`)
- [ ] Детали тура открываются
- [ ] Форма бронирования работает
- [ ] Уведомление приходит в Telegram
- [ ] Страница успеха отображается после заявки

---

## ❓ Частые проблемы

### Сайт не открывается
- Подождите 2-3 минуты после деплоя
- Проверьте статус билда в Vercel/Netlify
- Посмотрите логи сборки

### Telegram не отправляет
- Проверьте что токен правильный
- Убедитесь что отправили боту хотя бы одно сообщение
- Проверьте Chat ID (это число, не username)
- Посмотрите Function Logs в Vercel

### API не работает
- Проверьте что все 3 переменные окружения добавлены
- Пересоберите проект (Vercel: Deployments → ... → Redeploy)

---

## 🎨 Добавить свой домен

### Vercel:
1. Settings → Domains
2. Add Domain → введите `kamhub.ru`
3. Настройте DNS у регистратора:
   ```
   A     @     76.76.21.21
   CNAME www   cname.vercel-dns.com
   ```

### Netlify:
1. Domain settings → Add custom domain
2. Настройте DNS:
   ```
   A     @     75.2.60.5
   CNAME www   ваш-сайт.netlify.app
   ```

---

## 🚀 Автоматические обновления

После первого деплоя:

✅ **Каждый push в `main`** → автоматически деплоится  
✅ **Pull Request** → создаётся preview URL  
✅ **Rollback** → за 1 клик откатить к любой версии  

---

## 📱 Проверка на телефоне

После деплоя откройте URL на смартфоне - сайт адаптивный и работает на всех устройствах!

---

## 🎉 Готово!

**URL вашего сайта:**
- Vercel: `https://kamhub-xxx.vercel.app`
- Netlify: `https://kamhub-xxx.netlify.app`
- Свой сервер: `http://ваш-ip:3000`

**Делитесь с партнёрами и начинайте принимать заявки! 🎣🏔️**

---

## 📞 Нужна помощь?

- 📧 Email: info@kamhub.ru
- 💬 Telegram: @kamchatka_real
- 🐛 Issues: https://github.com/pospkam/HabKam/issues

**Время деплоя: 5 минут | Сложность: Легко | Стоимость: $0 (Free tier)**
