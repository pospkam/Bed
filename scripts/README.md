# 🛠️ Скрипты для Timeweb Cloud

Автоматизированные скрипты для упрощения развертывания и обслуживания KAMHUB на Timeweb Cloud.

---

## 📜 Доступные скрипты

### 1. `timeweb-install.sh` - Полная установка

**Описание:** Автоматически устанавливает все необходимое и разворачивает проект.

**Что делает:**
- ✅ Обновляет систему
- ✅ Устанавливает Node.js 18
- ✅ Устанавливает PM2, Nginx, Git
- ✅ Клонирует репозиторий
- ✅ Собирает проект
- ✅ Запускает приложение
- ✅ Настраивает Nginx

**Использование:**
```bash
# Скачать и запустить
curl -fsSL https://raw.githubusercontent.com/pospkam/HabKam/main/scripts/timeweb-install.sh | bash

# Или локально
bash scripts/timeweb-install.sh
```

**Время выполнения:** 5-10 минут

---

### 2. `timeweb-update.sh` - Обновление проекта

**Описание:** Быстрое обновление кода с GitHub без переустановки зависимостей.

**Что делает:**
- ✅ Останавливает приложение
- ✅ Получает последние изменения
- ✅ Обновляет зависимости
- ✅ Пересобирает проект
- ✅ Перезапускает приложение

**Использование:**
```bash
bash /var/www/HabKam/scripts/timeweb-update.sh

# Или создайте alias
echo "alias update-kamhub='bash /var/www/HabKam/scripts/timeweb-update.sh'" >> ~/.bashrc
source ~/.bashrc

# Теперь просто:
update-kamhub
```

**Время выполнения:** 2-3 минуты

---

### 3. `timeweb-ssl.sh` - Установка SSL

**Описание:** Автоматическая настройка HTTPS с Let's Encrypt.

**Что делает:**
- ✅ Устанавливает Certbot
- ✅ Получает SSL сертификат
- ✅ Настраивает автопродление
- ✅ Перенастраивает Nginx

**Требования:**
- Домен должен быть направлен на сервер
- DNS записи должны быть применены (2-4 часа)
- Порты 80 и 443 открыты

**Использование:**
```bash
bash /var/www/HabKam/scripts/timeweb-ssl.sh

# Скрипт запросит:
# 1. Ваш домен (например: kamhub.ru)
# 2. Добавить ли www поддомен (y/n)
# 3. Email для уведомлений
```

**Время выполнения:** 1-2 минуты

---

### 4. `timeweb-monitor.sh` - Мониторинг

**Описание:** Проверка статуса и здоровья системы.

**Что показывает:**
- 📊 Использование системных ресурсов
- 🚀 Статус PM2 процессов
- 🔍 Доступность приложения
- 📝 Последние логи
- ⚠️ Ошибки Nginx

**Использование:**
```bash
bash /var/www/HabKam/scripts/timeweb-monitor.sh

# Создайте alias для быстрого доступа
echo "alias monitor='bash /var/www/HabKam/scripts/timeweb-monitor.sh'" >> ~/.bashrc
source ~/.bashrc

# Теперь просто:
monitor
```

**Время выполнения:** моментально

---

## 🚀 Быстрый старт

### Новая установка (с нуля)

```bash
# 1. Создайте сервер Ubuntu 22.04 в Timeweb Cloud
# 2. Подключитесь по SSH
ssh root@ваш-ip

# 3. Запустите установку
curl -fsSL https://raw.githubusercontent.com/pospkam/HabKam/main/scripts/timeweb-install.sh | bash

# 4. Следуйте инструкциям (введите Telegram токены)
# 5. Готово! Сайт доступен по http://ваш-ip
```

### Обновление кода

```bash
# Подключитесь к серверу
ssh root@ваш-ip

# Обновите
bash /var/www/HabKam/scripts/timeweb-update.sh
```

### Добавление SSL

```bash
# Убедитесь что домен направлен на сервер
# Подождите 2-4 часа после настройки DNS

ssh root@ваш-ip
bash /var/www/HabKam/scripts/timeweb-ssl.sh
```

---

## 📋 Требования

**Минимальные:**
- Ubuntu 22.04 LTS
- 1 GB RAM
- 10 GB SSD
- Права root

**Рекомендуемые:**
- Ubuntu 22.04 LTS
- 2 GB RAM
- 20 GB SSD
- Права root

---

## ❓ FAQ

### Скрипт зависает на "Сборка проекта"

**Причина:** Недостаточно RAM для сборки Next.js

**Решение:**
```bash
# Добавьте SWAP
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab

# Повторите установку
```

### "Permission denied" при запуске

**Решение:**
```bash
# Сделайте скрипт исполняемым
chmod +x scripts/*.sh

# Запустите от root
sudo bash scripts/timeweb-install.sh
```

### SSL скрипт говорит "Домен не направлен"

**Причина:** DNS записи ещё не применились

**Решение:**
1. Проверьте DNS: `nslookup ваш-домен.ru`
2. Подождите 2-4 часа
3. Повторите попытку

### Приложение падает после обновления

**Решение:**
```bash
# Проверьте логи
pm2 logs kamhub

# Откатитесь к предыдущей версии
cd /var/www/HabKam
git log --oneline -5
git reset --hard <commit-hash>
npm run build
pm2 restart kamhub
```

---

## 🔧 Кастомизация скриптов

Все скрипты можно редактировать под свои нужды:

```bash
# Откройте скрипт
nano /var/www/HabKam/scripts/timeweb-install.sh

# Измените параметры
# Сохраните: Ctrl+X → Y → Enter
```

**Переменные окружения в скриптах:**
- `RED`, `GREEN`, `BLUE`, `YELLOW` - цвета вывода
- `SERVER_IP` - IP адрес сервера
- `DOMAIN` - ваш домен

---

## 📞 Поддержка

Если скрипты не работают:

1. Проверьте логи: `/var/log/nginx/kamhub-error.log`
2. Проверьте PM2: `pm2 logs kamhub`
3. Создайте Issue: https://github.com/pospkam/HabKam/issues

---

## 📄 Лицензия

MIT License - используйте свободно!
