# 📖 HabKam - Полное руководство пользователя

## 🎯 Что такое HabKam?

**HabKam** - агрегатор туров по Камчатке, который позволяет:
- **Партнёрам (операторам):** размещать свои туры и управлять датами
- **Клиентам:** просматривать туры и бронировать даты
- **Администраторам:** управлять всеми турами и партнёрами

---

## 🏗️ Архитектура системы

```
┌─────────────────────────────────────────────┐
│          ПУБЛИЧНАЯ ЧАСТЬ (/)                │
│  - Главная страница                          │
│  - Каталог туров (/tours)                    │
│  - Страница тура (/tours/[slug])             │
│  - Форма бронирования (/booking/[tourId])    │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│      АУТЕНТИФИКАЦИЯ (/login, /register)     │
│  - NextAuth v5                               │
│  - Email + Password                          │
│  - JWT сессии (30 дней)                      │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│       АДМИН-ПАНЕЛЬ ПАРТНЁРА (/partner)      │
│  - Список туров                              │
│  - Создание/редактирование туров             │
│  - Управление датами (календарь)             │
│  - Просмотр бронирований                     │
└─────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────┐
│           BASE ДАННЫХ (PostgreSQL)          │
│  - Партнёры (Partner)                        │
│  - Туры (Tour)                               │
│  - Даты (TourDate)                           │
│  - Бронирования (Booking)                    │
└─────────────────────────────────────────────┘
```

---

## 👥 Роли пользователей

### 1. **Гость (не авторизован)**
- ✅ Просмотр туров
- ✅ Бронирование туров
- ❌ Управление турами

### 2. **PARTNER (партнёр)**
- ✅ Всё что гость
- ✅ Создание своих туров
- ✅ Редактирование своих туров
- ✅ Управление датами своих туров
- ✅ Просмотр своих бронирований
- ❌ Доступ к чужим турам

### 3. **ADMIN (администратор)**
- ✅ Всё что партнёр
- ✅ Доступ ко ВСЕМ турам
- ✅ Управление партнёрами
- ✅ Просмотр всех бронирований
- ✅ Глобальные настройки

---

## 🚀 Быстрый старт

### Для клиентов (бронирование тура):

1. Откройте сайт: `https://habkam.ru` или `http://185.84.163.199:3000`
2. Перейдите в каталог туров: `/tours`
3. Выберите интересующий тур
4. На странице тура нажмите на доступную дату
5. Заполните форму бронирования
6. Получите подтверждение в Telegram

### Для партнёров (добавление тура):

1. Зарегистрируйтесь: `/register`
2. Войдите: `/login`
3. Перейдите в панель: `/partner/tours`
4. Нажмите **"Создать тур"**
5. Заполните информацию о туре
6. Добавьте доступные даты
7. Тур появится в публичном каталоге

---

## 🔐 Аутентификация

### Регистрация партнёра

**URL:** `/register`

**Форма:**
```
Имя/Название компании: _________________
Email:                  _________________
Пароль:                 _________________
Телефон (опц):          _________________
Telegram (опц):         _________________
```

**После регистрации:**
- Автоматический вход в систему
- Редирект на `/partner/dashboard`
- Роль: `PARTNER`

### Вход

**URL:** `/login`

**Тестовые данные:**
```
Партнёр:
  Email: partner@fishingkam.ru
  Пароль: demo123456

Админ:
  Email: admin@habkam.ru
  Пароль: admin123456
```

**Сессия:**
- Длительность: 30 дней
- Технология: JWT
- Хранение: HTTP-only cookie

---

## 📊 Публичная часть

### Главная страница `/`

**Элементы:**
- Hero секция с призывом к действию
- Популярные туры (карточки)
- Категории туров
- Footer с контактами

### Каталог туров `/tours`

**Возможности:**
- Просмотр всех туров
- Фильтрация по категории
- Сортировка по цене
- Поиск по названию

**Карточка тура:**
```
┌─────────────────────────────┐
│     [Изображение тура]      │
│                             │
│ 🎣 Зимняя рыбалка           │
│                             │
│ 📍 Река Камчатка            │
│ 👥 2-6 человек              │
│ 💰 18,000₽/сутки            │
│                             │
│     [Подробнее →]           │
└─────────────────────────────┘
```

### Страница тура `/tours/[slug]`

**Разделы:**

**1. Галерея изображений**
```
┌─────┬─────┬─────┐
│  1  │  2  │  3  │
└─────┴─────┴─────┘
```

**2. Описание:**
- Название тура
- Категория и сложность
- Полное описание
- Что входит в стоимость
- Что НЕ входит в стоимость
- Информация об операторе

**3. Бронирование (sidebar):**
```
┌─────────────────────────────┐
│   Забронировать             │
│                             │
│   18,000₽ /сутки            │
│                             │
│   Доступные даты:           │
│   🟢 15-20 февраля  6 мест  │
│   🟢 25 фев - 3 мар  6 мест │
│   🟡 10-15 марта    2 места │
│                             │
│   [Забронировать →]         │
└─────────────────────────────┘
```

**При клике на дату:**
- Открывается модальное окно
- Форма с предзаполненной датой
- Поля: имя, телефон, email, кол-во человек

### Форма бронирования `/booking/[tourId]`

**Поля:**
```
Выберите даты:          [○ 15-20 февраля]
                        [○ 25 фев - 3 мар]

Количество человек:     [2 ▼]

Ваше имя:              [_____________]
Телефон:               [_____________]
Email:                 [_____________]
Telegram (опц):        [_____________]
Комментарий:           [_____________]

Итого: 270,000₽
(18,000₽/сутки × 5 дней × 3 чел)

          [Отправить заявку]
```

**После отправки:**
- Сохранение в БД
- Отправка уведомления в Telegram бот
- Редирект на `/booking/success`

---

## 🎛️ Админ-панель партнёра

### Dashboard `/partner/dashboard`

**Статистика:**
```
┌───────────────┬───────────────┬───────────────┐
│ Мои туры      │ Доступ. даты  │ Брони         │
│      2        │      5        │     12        │
└───────────────┴───────────────┴───────────────┘
```

**Быстрые действия:**
- ➕ Добавить новый тур
- 📅 Управление датами

### Список туров `/partner/tours`

**Представление:**
```
┌─────────────────────────────────────────────┐
│         [Изображение тура]                  │
│                                             │
│  🎣 Зимняя рыбалка на Камчатке              │
│  Увлекательная рыбалка на льду...           │
│                                             │
│  📅 5 дат | 18,000₽/сутки                   │
│                                             │
│  Ближайшая: 15 февраля 2026                 │
│                                             │
│  [Смотреть] [Изменить] [Даты]               │
└─────────────────────────────────────────────┘
```

**Действия:**
- **Смотреть** → Публичная страница тура
- **Изменить** → Редактирование `/partner/tours/[id]/edit`
- **Даты** → Календарь дат `/partner/tours/[id]/dates`

### Создание тура `/partner/tours/create`

**API:** `POST /api/partner/tours`

**Обязательные поля:**
```javascript
{
  title: "Зимняя рыбалка на Камчатке",        // min 5 символов
  slug: "winter-fishing-kamchatka",          // уникальный, min 3
  description: "Краткое описание...",         // min 20 символов
  locationName: "Река Камчатка",              // min 2 символа
  category: "FISHING",                        // enum
  difficulty: "MEDIUM",                       // enum
  pricePerDay: 18000,                        // > 0
  minGroupSize: 2,                           // >= 1
  maxGroupSize: 6,                           // >= minGroupSize
  minDuration: 3,                            // >= 1
  included: ["Снаряжение", "Транспорт"],    // массив
  notIncluded: ["Питание", "Страховка"]      // массив
}
```

**Опциональные поля:**
```javascript
{
  fullDescription: "Полное описание...",
  priceOriginal: 25000,                      // зачёркнутая цена
  images: ["https://...", "https://..."]     // массив URL
}
```

**Категории:**
- `FISHING` - 🎣 Рыбалка
- `HIKING` - 🥾 Походы
- `VOLCANO` - 🌋 Вулканы
- `SKIING` - ⛷️ Горные лыжи
- `OTHER` - 🏔️ Приключения

**Сложность:**
- `EASY` - Легкий
- `MEDIUM` - Средний
- `HARD` - Сложный

### Редактирование тура `/partner/tours/[id]/edit`

**API:** `PUT /api/partner/tours/[id]`

**Особенности:**
- Все поля опциональны
- Можно обновить только нужные поля
- Проверка уникальности slug
- Проверка владения (только свой тур)

**Пример частичного обновления:**
```javascript
{
  pricePerDay: 20000,      // изменили только цену
  maxGroupSize: 8          // и размер группы
}
```

### Управление датами `/partner/tours/[id]/dates`

**API:** `POST /api/partner/tours/[id]/dates`

**Добавление дат (массово):**
```javascript
{
  dates: [
    {
      startDate: "2026-02-15",
      endDate: "2026-02-20",
      status: "AVAILABLE",          // по умолчанию
      spotsTotal: 6,                 // по умолчанию
      spotsAvailable: 6,             // по умолчанию = spotsTotal
      priceOverride: 20000,          // опц: переопределить цену
      notes: "Праздничный период"    // опц: заметки
    },
    {
      startDate: "2026-03-01",
      endDate: "2026-03-05"
      // остальные поля по умолчанию
    }
  ]
}
```

**Статусы дат:**
- `AVAILABLE` - 🟢 Доступно (все места свободны)
- `PARTIAL` - 🟡 Частично (есть свободные места)
- `BOOKED` - 🔴 Забронировано (мест нет)
- `BLOCKED` - ⚫ Заблокировано (недоступно для бронирования)

**Обновление конкретной даты:**

**API:** `PUT /api/partner/tours/[id]/dates/[dateId]`

```javascript
{
  spotsAvailable: 3,        // уменьшили кол-во мест
  status: "PARTIAL",        // изменили статус
  notes: "Осталось 3 места"
}
```

**Удаление даты:**

**API:** `DELETE /api/partner/tours/[id]/dates/[dateId]`

---

## 🗄️ База данных

### Модель Partner (Партнёр)

```prisma
model Partner {
  id            String    @id @default(cuid())
  name          String
  email         String    @unique
  passwordHash  String
  role          Role      @default(PARTNER)  // PARTNER или ADMIN
  phone         String?
  telegram      String?
  
  tours         Tour[]
  
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}
```

### Модель Tour (Тур)

```prisma
model Tour {
  id              String       @id @default(cuid())
  slug            String       @unique
  title           String
  description     String
  fullDescription String?
  
  locationName    String
  category        TourCategory
  difficulty      Difficulty
  
  pricePerDay     Int
  priceOriginal   Int?
  
  minGroupSize    Int
  maxGroupSize    Int
  minDuration     Int
  
  included        String[]     // массив
  notIncluded     String[]     // массив
  images          String[]     // массив URL
  
  partnerId       String
  partner         Partner      @relation(...)
  
  dates           TourDate[]
  bookings        Booking[]
}
```

### Модель TourDate (Дата тура)

```prisma
model TourDate {
  id              String      @id @default(cuid())
  tourId          String
  
  startDate       DateTime
  endDate         DateTime
  
  status          DateStatus
  spotsTotal      Int         @default(6)
  spotsAvailable  Int         @default(6)
  
  priceOverride   Int?
  notes           String?
  
  @@unique([tourId, startDate])  // уникальность
}
```

### Модель Booking (Бронирование)

```prisma
model Booking {
  id              String        @id @default(cuid())
  tourId          String
  tourTitle       String
  
  startDate       DateTime
  endDate         DateTime
  days            Int
  
  customerName    String
  customerPhone   String
  customerEmail   String?
  customerTelegram String?
  
  persons         Int
  comment         String?
  
  pricePerDay     Int
  totalPrice      Int
  commission      Int           @default(0)  // 10%
  
  status          BookingStatus @default(PENDING)
  source          String        @default("website")
  
  utmSource       String?       // для трекинга
  utmMedium       String?
  
  createdAt       DateTime      @default(now())
}
```

---

## 🔌 API Reference

### Публичное API

#### GET `/api/tours`
Получить все туры.

#### GET `/api/tours/[id]`
Получить конкретный тур.

#### GET `/api/dates/[tourId]`
Получить доступные даты тура (только будущие).

#### POST `/api/bookings`
Создать бронирование.

### Защищённое API (требует авторизации)

#### GET `/api/partner/tours`
Список своих туров (или всех для ADMIN).

#### POST `/api/partner/tours`
Создать тур.

#### GET `/api/partner/tours/[id]`
Получить тур с деталями.

#### PUT `/api/partner/tours/[id]`
Обновить тур.

#### DELETE `/api/partner/tours/[id]`
Удалить тур (если нет броней).

#### POST `/api/partner/tours/[id]/dates`
Добавить даты (массово).

#### PUT `/api/partner/tours/[id]/dates/[dateId]`
Обновить дату.

#### DELETE `/api/partner/tours/[id]/dates/[dateId]`
Удалить дату.

---

## 📲 Интеграция с Telegram

### Уведомления о бронированиях

При создании бронирования отправляется сообщение в Telegram бот:

```
🎉 Новое бронирование!

📝 Тур: Зимняя рыбалка на Камчатке
📅 Даты: 15-20 февраля 2026 (5 дней)
👤 Клиент: Иван Иванов
📞 Телефон: +7 999 123-45-67
📧 Email: ivan@mail.ru
👥 Человек: 3
💰 Сумма: 270,000₽

💬 Комментарий: Нужна встреча в аэропорту

🆔 ID: clx...
🕐 Создано: 10.02.2026 15:30
```

### Настройка бота

1. Создайте бота через @BotFather
2. Получите токен
3. Добавьте в `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

---

## 🚀 Деплой

### Автоматический деплой

```bash
# На сервере
cd /var/www/habkam
bash scripts/deploy-timeweb.sh
```

**Скрипт выполняет:**
1. ✅ Git pull (обновление кода)
2. ✅ npm ci (установка зависимостей)
3. ✅ Prisma generate
4. ✅ DB push (синхронизация схемы)
5. ✅ npm run build
6. ✅ PM2 restart

### Ручной деплой

```bash
ssh root@185.84.163.199
cd /var/www/habkam
git pull origin main
npm install
npm run db:generate
npm run db:push
npm run build
pm2 restart habkam
```

---

## ✅ Чеклист запуска

### Перед первым запуском:

- [ ] PostgreSQL база создана
- [ ] `.env` файл настроен
- [ ] `npm install` выполнен
- [ ] `npm run db:generate` выполнен
- [ ] `npm run db:push` выполнен
- [ ] `npm run db:seed` выполнен (тестовые данные)
- [ ] `npm run build` прошёл успешно
- [ ] PM2 настроен и запущен

### Проверка работоспособности:

- [ ] Главная страница открывается
- [ ] Список туров отображается
- [ ] Страница тура работает
- [ ] Модальное окно бронирования открывается
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Админ-панель открывается после входа
- [ ] API туров работает
- [ ] API дат работает

---

## 📞 Контакты и поддержка

**GitHub:** https://github.com/pospkam/HabKam

**Документация:**
- `docs/NEXTAUTH_SETUP.md` - Аутентификация
- `docs/ADMIN_PANEL.md` - Админ-панель
- `docs/TIMEWEB_POSTGRESQL_SETUP.md` - База данных

**Telegram:** @kamchatka_real

---

## 🎉 Готово к использованию!

Система полностью настроена и готова принимать бронирования! 🚀
