# Подключение Timeweb Managed PostgreSQL к проекту HabKam

## 📋 Шаг 1: Создание базы данных в Timeweb Cloud

### 1.1. Войдите в панель Timeweb Cloud
- Перейдите на https://timeweb.cloud
- Войдите в свой аккаунт

### 1.2. Создайте PostgreSQL базу данных
1. В меню слева выберите **"Базы данных"**
2. Нажмите **"Создать базу данных"**
3. Выберите:
   - **Тип:** PostgreSQL
   - **Версия:** 16.x (рекомендуется последняя)
   - **Конфигурация:** Начните с минимальной (можно масштабировать позже)
   - **Имя:** `habkam_db`
   - **Регион:** Москва (для минимальной задержки)
4. Нажмите **"Создать"**
5. Дождитесь завершения создания (обычно 2-5 минут)

### 1.3. Получите данные для подключения
После создания БД вы получите:
- **Хост:** `pg-xxxxx.timeweb.cloud` или IP адрес
- **Порт:** обычно `5432`
- **Имя БД:** `habkam_db`
- **Пользователь:** `habkam_user` (автоматически создан)
- **Пароль:** скопируйте и сохраните в надёжном месте

---

## 📋 Шаг 2: Настройка проекта

### 2.1. Создайте файл `.env` в корне проекта

```bash
cp .env.example .env
```

### 2.2. Заполните DATABASE_URL

Откройте `.env` и замените строку подключения:

```env
DATABASE_URL="postgresql://habkam_user:YOUR_PASSWORD@pg-xxxxx.timeweb.cloud:5432/habkam_db?schema=public"
```

**Формат:**
```
postgresql://[USER]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]?schema=public
```

**Пример с реальными данными:**
```env
DATABASE_URL="postgresql://habkam_user:Kj8x2Qa9Pw5@pg-12345.timeweb.cloud:5432/habkam_db?schema=public"
```

---

## 📋 Шаг 3: Инициализация базы данных

### 3.1. Генерация Prisma Client

```bash
npm run db:generate
```

Эта команда создаст типизированный клиент Prisma на основе `schema.prisma`.

### 3.2. Применение схемы к базе данных

**Вариант A: db:push (быстрый, для разработки)**
```bash
npm run db:push
```

**Вариант B: migrate (рекомендуется для production)**
```bash
npm run db:migrate
```

При выборе варианта B вас попросят назвать миграцию:
```
Enter a name for the new migration: » initial_setup
```

### 3.3. Заполнение тестовыми данными (seed)

```bash
npm run db:seed
```

Этот скрипт:
- ✅ Создаст партнёра `partner@fishingkam.ru` (пароль: `demo123456`)
- ✅ Создаст админа `admin@habkam.ru` (пароль: `admin123456`)
- ✅ Мигрирует все туры из `data/tours.json`
- ✅ Мигрирует все доступные даты из `data/dates.json`

---

## 📋 Шаг 4: Проверка подключения

### 4.1. Откройте Prisma Studio (GUI для БД)

```bash
npm run db:studio
```

Откроется браузер с адресом `http://localhost:5555`

### 4.2. Проверьте таблицы:
- `partners` - должно быть 2 записи (партнёр + админ)
- `tours` - туры из JSON
- `tour_dates` - доступные даты
- `bookings` - пока пусто

---

## 🔐 Безопасность

### Важно:
1. ✅ Добавьте `.env` в `.gitignore` (уже добавлено)
2. ✅ Никогда не коммитьте файл `.env` с реальными паролями
3. ✅ Используйте `.env.example` как шаблон
4. ✅ В production используйте переменные окружения сервера

### Для Timeweb Cloud VPS:
Создайте `.env` на сервере:

```bash
# SSH на сервер
ssh root@185.84.163.199

# Перейдите в директорию проекта
cd /var/www/habkam

# Создайте .env
nano .env

# Вставьте реальные данные и сохраните (Ctrl+X, Y, Enter)
```

---

## 🛠️ Полезные команды

```bash
# Генерация Prisma Client после изменения schema.prisma
npm run db:generate

# Применить изменения схемы (без миграций)
npm run db:push

# Создать новую миграцию
npm run db:migrate

# Заполнить БД тестовыми данными
npm run db:seed

# Открыть Prisma Studio (GUI)
npm run db:studio

# Сбросить БД (удалить все данные и пересоздать)
npx prisma migrate reset

# Посмотреть статус миграций
npx prisma migrate status
```

---

## ❓ Troubleshooting

### Ошибка: "Can't reach database server"
- Проверьте, что БД запущена в Timeweb Cloud
- Проверьте правильность хоста и порта
- Убедитесь, что IP вашего сервера/компьютера добавлен в whitelist (если настроен файрвол)

### Ошибка: "Authentication failed"
- Проверьте правильность пароля в DATABASE_URL
- Убедитесь, что нет лишних пробелов

### Ошибка при seed: "Unique constraint failed"
- Скорее всего данные уже существуют
- Сбросьте БД: `npx prisma migrate reset` и повторите seed

---

## 📊 Мониторинг

### Timeweb Cloud Dashboard:
- **CPU/RAM:** следите за нагрузкой
- **Storage:** проверяйте место на диске
- **Connections:** количество активных подключений

### Рекомендации:
- Начните с минимального тарифа
- Масштабируйте по мере роста нагрузки
- Настройте автоматические бэкапы (в настройках БД)

---

## ✅ Готово!

После выполнения всех шагов у вас будет:
- ✅ PostgreSQL база данных в Timeweb Cloud
- ✅ Prisma ORM подключена и настроена
- ✅ Тестовые данные загружены
- ✅ Готово к разработке функционала аутентификации и админ-панели

**Следующий шаг:** Переходите к **Шагу 2: Аутентификация (NextAuth v5)**
